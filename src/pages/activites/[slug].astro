---
import Layout from "../../layouts/Layout.astro";
import ActiviteDetail from "../../components/detail/ActiviteDetail.jsx";
import { activites } from "../../data/activites.js";

export function getStaticPaths() {
  return activites.map((activite) => ({
    params: { slug: activite.slug },
    props: { activite },
  }));
}

const { activite } = Astro.props;
---

<Layout title={`${activite.nom} - Activités et loisirs - Castagniccia Casinca`}>
  <ActiviteDetail activite={activite} client:load />
</Layout>
